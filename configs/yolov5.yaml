model:
  name: "YOLOv5"
  version: "s"
  num_classes: 80
  input_size: [640, 640]
  act: "silu"
data:
  data_dir: "D:/Datasets/coco/coco128"
  output_dir: "./outputs"
  train_json: "train.json"
  val_json: "train.json"
  test_json: null
  batch_size: 16
  max_epochs: 20
  experiment_name: "train"
testing:
  conf_thresh: 0.001
  iou_thresh: 0.6
hypermeters:
  lr0: 0.01  # initial learning rate (SGD=1E-2, Adam=1E-3)
  lrf: 0.01  # final OneCycleLR learning rate (lr0 * lrf)
  momentum: 0.937  # SGD momentum/Adam beta1
  weight_decay: 0.0005  # optimizer weight decay 5e-4
  warmup_epochs: 3.0  # warmup epochs (fractions ok)
  warmup_momentum: 0.8  # warmup initial momentum
  warmup_bias_lr: 0.1  # warmup initial bias lr
  basic_lr_per_img: 0.00015625  # learning rate for one image. During training, lr will multiply batchsize.
  min_lr_ratio: 0.05
  no_aug_epochs: 15
  box: 0.05  # box loss gain
  cls: 0.5  # cls loss gain
  cls_pw: 1.0  # cls BCELoss positive_weight
  obj: 1.0  # obj loss gain (scale with pixels)
  obj_pw: 1.0  # obj BCELoss positive_weight
  iou_t: 0.20  # IoU training threshold
  anchor_t: 4.0  # anchor-multiple threshold
  fl_gamma: 0.0  # focal loss gamma (efficientDet default gamma=1.5)
augmentations:
  degrees: 10.0  # image rotation (+/- deg)
  translate: 0.1  # image translation (+/- fraction)
  scale: 0.5  # image scale (+/- gain)
  shear: 2.0  # image shear (+/- deg)
  perspective: 0.0  # image perspective (+/- fraction), range 0-0.001
  flip_ud_prob: 0.0  # image flip up-down (probability)
  flip_lr_prob: 0.5  # image flip left-right (probability)
  mosaic_prob: 1.0  # image mosaic (probability)
  mixup_prob: 1.0  # image mixup (probability)
  hsv_prob: 1.0 # image hsv (probability)
  mosaic_scale: [0.1, 2]
  mixup_scale: [0.5, 1.5]
  enable_mixup: true
  enable_mosaic: true